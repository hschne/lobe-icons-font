{
  "name": "lobe-icons-font",
  "version": "5.10.0",
  "private": true,
  "description": "Glyph font of lobehub AI/LLM provider icons for terminals and waybar",
  "license": "MIT",
  "engines": {
    "node": ">=26"
  },
  "packageManager": "npm@11.17.0",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tmp=$(mktemp -d) && cp codepoints.json dist/lobe-icons.ttf \"$tmp/\" && npm run build && cmp \"$tmp/codepoints.json\" codepoints.json && cmp \"$tmp/lobe-icons.ttf\" dist/lobe-icons.ttf"
  },
  "dependencies": {
    "@lobehub/icons": "5.10.0"
  },
  "devDependencies": {
    "svg2ttf": "6.1.0",
    "svgicons2svgfont": "12.0.0",
    "svgpath": "2.6.0"
  }
}
